ROZPOČTOVÉ HLÁŠENÍ: všech 5 průchodů dokončeno. Průchod 5 uvádím v plném rozsahu (251 jmen ve 126 souborech) v příloze A, ne v hlavní tabulce — do tabulky jsem vytáhl jen ty, které stojí pod zákaznicky viditelnou plochou. Nic není tiše zkráceno.

    3d8b581a3cdcac1058d3fe4b0190944e7fb1b450
    ?? docs/MAILCHIMP-VS-MAILFORGE-2026-08-19.md

Sonda nad pracovní kopií, bez přepínání větví. Žádná změna kódu, žádná větev, žádný commit. Jediný zápis na disk je tento soubor.

---

## Nálezy

Řazeno VIDITELNÉ → NEJASNÉ → SKRYTÉ. Uvnitř skupiny podle závažnosti.

| funkce                                                     | vstupní bod (soubor:řádek)                                                                                                                                  | co chybí                                                                                                                                                                                                                                                                                                                        | důkaz (příkaz + počet zásahů)                                                                                                                                                                                     | co uvidí zákazník                                                                                                                                                                                                   |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Workflow s triggerem „Form submit"**                     | `apps/web/.../workflows/new/new-workflow-form.tsx:17` (volba v UI) → `routes/v1/workflows.ts:44` (API ji přijme) → `db/schema/workflows.ts:40` (enum ji má) | Nikdo se na tenhle typ nikdy nedotáže. Neexistuje `onFormSubmit` ani `startTypedWorkflows(…,'form_submit',…)`. Odeslání formuláře spustí workflow jen tehdy, když má **formulář** v configu `workflowId` → `triggerManual` (`services/signup-forms/index.ts:335-337`).                                                          | `grep -rn "triggerType, 'form_submit'\|startTypedWorkflows(.*form_submit" --include=*.ts apps/api/src` → **0** · `grep -rn 'onFormSubmit' --include=*.ts apps/` → **0**                                           | **VIDITELNÉ** — uživatel vybere „Form submit", workflow aktivuje, a ono nikdy neběží. Tři šablony v galerii (`webinar-registration`, `whitepaper-download`, `in-person-event-rsvp`) po forku vzniknou trvale spící. |
| **Inbox Preview bez Litmusu**                              | `apps/web/.../inbox-preview/[id]/page.tsx:99` (`<img src={r.thumbnailUrl}>`) → `services/preview/providers.ts:170`                                          | Bez `LITMUS_API_KEY` se **tiše** vybere `mockProvider`, který vrací `status: 'completed'` a URL na `https://preview.mock.local/...` — neexistující doména.                                                                                                                                                                      | `grep -n 'selectProvider' -A 4 services/preview/providers.ts:167-170` → fallback `return mockProvider` bez varování · `mockProvider.pollJob` (`providers.ts:65-79`) vrací natvrdo 3 klienty                       | **VIDITELNÉ** — náhled se tváří jako hotový a zobrazí tři rozbité obrázky.                                                                                                                                          |
| **Geo statistiky kampaně**                                 | `apps/web/.../campaigns/[id]/page.tsx` volá `/api/v1/campaigns/${id}/stats/geo` → `lib/geo.ts:68-69`                                                        | `resolveGeo` je bez `GEOIP_API_URL` no-op (`if (!template …) return null`). Proměnná není v docker-compose ani v CI.                                                                                                                                                                                                            | `grep -rn 'GEOIP_API_URL' docker-compose.yml docker-compose.prod.yml` → **0** · v `.env.example` je (1×), v `.env.compose.example` **0**                                                                          | **VIDITELNÉ** — panel „podle země / města / mapa" je v UI, ale trvale prázdný.                                                                                                                                      |
| **Rezervace schůzky s Google Meet**                        | `services/meetings/video-links.ts:94-101` (volá se z `services/meetings/index.ts:210`)                                                                      | Nic se u Googlu nezakládá. Funkce **vymyslí náhodný kód** a vrátí `https://meet.google.com/xxx-xxxx-xxx`. Rezervace se uloží `status:'confirmed'`.                                                                                                                                                                              | `sed -n '94,109p' services/meetings/video-links.ts` — `generateMeetCode()` = `Math.random()` nad abecedou; žádný `fetch` na Google API                                                                            | **VIDITELNÉ** — `/meetings` ukazuje potvrzenou schůzku s odkazem, který nikam nevede.                                                                                                                               |
| **Rezervace schůzky se Zoomem / Teams**                    | `services/meetings/video-links.ts:50`, `:117`                                                                                                               | Bez `ZOOM_*` / `MICROSOFT_*` vrací `null`; `index.ts:216` to spolkne (`.catch(() => …)`) a uloží rezervaci `status:'confirmed'` s `meetingUrl: null`.                                                                                                                                                                           | `grep -n 'if (!accountId \|\| !clientId \|\| !clientSecret) return null' video-links.ts:50` · žádná z proměnných není v docker-compose (**0**)                                                                    | **VIDITELNÉ** — potvrzená schůzka bez odkazu, bez chybové hlášky.                                                                                                                                                   |
| **Kaskádový e-mail a SMS ve workflow**                     | `services/workflows/actions.ts:1021` (`cascade-email`), `:1034` (`cascade-sms`)                                                                             | Payloady neodpovídají kontraktu příjemce. `cascade-email` posílá jen `{orgId, contactId, workflowRunId}` — endpoint `internal/workflow/send-email` bez `campaignId`/`templateId`/`html` vrací **400** (`workflow-dispatch.ts:138`). `cascade-sms` nemá `message`, které je v zod schématu povinné (`workflow-dispatch.ts:178`). | `sed -n '1014,1042p' actions.ts` vs `sed -n '66,76p' + '173,185p' routes/v1/internal/workflow-dispatch.ts`                                                                                                        | **VIDITELNÉ** — kaskáda v UI proběhne „úspěšně", kontakt nedostane nic. Chyba skončí v logu workeru po 3 pokusech.                                                                                                  |
| **Kaskáda přes push a WhatsApp**                           | `services/workflows/actions.ts:1043-1046`                                                                                                                   | `case 'push': case 'whatsapp':` obsahuje jen komentář `// Placeholder for push/whatsapp queuing` a `break`. Celá funkce je navíc v `try { } catch { }` bez logu (`:1063` „Silently fail if queues not available").                                                                                                              | `sed -n '1043,1065p' actions.ts`                                                                                                                                                                                  | **VIDITELNÉ** — kanál jde v editoru vybrat, neodešle se nic a nikde to není vidět.                                                                                                                                  |
| **Stripe retry v šabloně „dunning"**                       | `services/workflows/actions.ts:1386-1389`, šablona `services/workflows/templates/dunning.ts:62-63` a `:85-86`                                               | Akce čte `invoiceId` z `node.config` **nebo** `run.data`. Šablona v configu posílá jen `{description}` a `run.data.invoiceId` nikdo nikdy nezapisuje → `if (!invoiceId) return next`.                                                                                                                                           | `grep -rn 'invoiceId' services/workflows/ \| grep -v .test.` → **5 zásahů, všechny čtení** · `grep -n "stripe_retry_charge" -A 4 templates/dunning.ts`                                                            | **VIDITELNÉ** — obě „Retry failed invoice" větve dunning šablony jsou no-op, běh se tváří jako úspěšný.                                                                                                             |
| **`{{cart_url}}` a `{{review_url}}` v ticketing workflow** | `services/ticketing/seed-workflows.ts:42`, `:52`                                                                                                            | Cron plní do `run.data` jen `externalEventId`, `eventTitle`, `event_url`, `ticket_url` (`services/ticketing/cron.ts:69-74`). Neznámý tag se renderuje jako prázdný řetězec (`apps/editor/src/render/merge-tags.ts:249`).                                                                                                        | `grep -n 'cart_url\|review_url' services/ticketing/cron.ts` → **0**                                                                                                                                               | **VIDITELNÉ** — příjemce dostane `<a href="">Dokončit objednávku →</a>`, tedy tlačítko, které nikam nevede.                                                                                                         |
| **E-mailový krok v prodejní sekvenci (CRM)**               | `services/crm/sales-sequences.ts:324`                                                                                                                       | Payload nese `bodyHtml`/`bodyText`, endpoint zná jen `html`/`text` (zod je zahodí) a pak spadne na `campaignId or templateId required` → **400**. `.catch(() => {})` na `:333` navíc polyká i selhání enqueue.                                                                                                                  | `sed -n '315,338p' sales-sequences.ts` vs `workflow-dispatch.ts:66-76`                                                                                                                                            | **VIDITELNÉ** — sekvence se posouvá dál, e-mail neodejde. SMS krok (`:352`) je naopak v pořádku.                                                                                                                    |
| **Reputace dedikované IP**                                 | `GET /api/v1/dedicated-ips` (`routes/v1/dedicated-ips.ts:123`) čte `reputationScore`                                                                        | `updateReputation`, `recordIpSend` a `dailyIpMaintenance` nemají volající. Sloupec `reputation_score` má default `'0'` a nic ho nikdy nepřepíše.                                                                                                                                                                                | `grep -rn 'updateReputation' --include=*.ts apps/ \| grep -v 'dist/\|.test.\|dedicated-ips/index.ts:'` → **0**                                                                                                    | **VIDITELNÉ (API)** — každá IP hlásí reputaci `0.00` napořád; report izolace subúčtů (`dedicated-ips/index.ts:411-439`) průměruje samé nuly.                                                                        |
| **IP warmup — vynucení objemu**                            | `GET /api/v1/sending/warmup`, `GET /api/v1/domains/:id/warmup`                                                                                              | Warmup se nikde v odesílací cestě nekontroluje. V TS jsou `checkWarmupAllowance` i `recordWarmupSend` bez volajícího; v Go se zapne jen když je nastavené `SENDING_IPS` **i** `REDIS_URL` (`apps/engine/main.go:43`). `SENDING_IPS` nenastavuje nic.                                                                            | `grep -rn 'warmup' apps/workers/src/jobs/batch-sender.ts apps/workers/src/jobs/mta-sender.ts` → **0** · `grep -rn 'SENDING_IPS' docker-compose*.yml .env.example` → **0**                                         | **VIDITELNÉ (API)** — `warmupDay` se denním cronem poctivě posouvá, takže warmup vypadá, že běží; žádný denní strop se ale neuplatní.                                                                               |
| **Fallback kanálu při bounce**                             | `services/campaigns/channel-fallback.ts:106-118`, routa `routes/v1/channel-fallback.ts`                                                                     | Funkce zapíše řádek do `channel_fallback_log` se `status: 'queued'` a skončí. Enqueue je **zakomentovaný**: `// await queue.add('channel-fallback', …)`.                                                                                                                                                                        | `sed -n '106,118p' channel-fallback.ts` — řádek 117 je komentář                                                                                                                                                   | **VIDITELNÉ (API)** — log fallbacků plný položek `queued`, které se nikdy nepošlou.                                                                                                                                 |
| **Hlasový hovor jako krok workflow**                       | `services/workflows/actions.ts:1574` → `executeQueuedChannel('voice', …)`                                                                                   | Fronta `voice-call` nemá nikde workera. Kód to sám ví a od `:640` vrací chybu místo tichého úspěchu.                                                                                                                                                                                                                            | `grep -rn "'voice-call'" --include=*.ts apps/ \| grep -v dist/` → **1** (jen `lib/queues.ts:70`, tedy definice fronty)                                                                                            | **VIDITELNÉ** — běh workflow spadne s „voice: queue has no running consumer". Alespoň nelže; hovor se nikdy neuskuteční.                                                                                            |
| **Nejlepší hodina odeslání pro kontakt**                   | `GET /api/v1/send-optimization/best-hour/:contactId` (`routes/v1/send-optimization.ts:40`)                                                                  | `bestHourForContact` čte `contact_engagement.open_hour_histogram`. Ten plní jedině `recordOpen`, a toho volá jen ruční endpoint `POST /send-optimization/record-open`. Pixel otevření (`routes/v1/tracking.ts:69-95`) ho nevolá.                                                                                                | `grep -rn 'open_hour_histogram\|openHourHistogram' --include=*.ts apps/ \| grep -v dist/` → **6 zásahů, jediný zapisující je `send-optimization/index.ts:39`**                                                    | **NEJASNÉ** — endpoint pro každý kontakt vrátí `{hour: 10}` (fallback). V `apps/web` jsem volání nenašel, takže dnes to vidí jen konzument API.                                                                     |
| **Per-contact STO (`batch-compute`)**                      | `POST /api/v1/send-optimization/batch-compute` (`routes/v1/per-contact-sto.ts:58`), `GET /api/v1/contacts/:id/best-send-time` (`:16`)                       | Predikce nikdo nepočítá. `computeContactSendTime` má **nula** volajících mimo tenhle jeden soubor — žádný cron, žádný worker, žádné volání z webu. GET tedy vrací `{data: null}`, dokud si někdo ručně nezavolá POST. Splitter kampaní předává jen `timewarp`, ne STO hodinu (`campaign-splitter.ts:158`, `:254`).              | `grep -rn 'computeContactSendTime' --include=*.ts --include=*.tsx apps/ \| grep -v 'dist/\|.test.\|per-contact-sto.ts:'` → **0** · `grep -rn 'best-send-time\|batch-compute' apps/web/src` → **0**                | **NEJASNÉ** — potvrzuje tvou indicii. Přes API prázdno, v UI to zatím není. `nba-engine.ts:73` kvůli tomu vždy spadne na heuristiku 10/14 h.                                                                        |
| **Archivace / retence e-mailových událostí**               | `POST /api/v1/internal/archive/email-events`, worker `apps/workers/src/jobs/archive-email-events.ts:17`                                                     | Worker běží, ale do fronty `archive-email-events` **nikdo nikdy nic nepřidá**. Hlavička souboru tvrdí „Runs nightly (scheduled via BullMQ cron)" — žádná `scheduleArchive()` neexistuje a `index.ts` volá jen `startArchiveWorker()`.                                                                                           | `grep -rn 'ARCHIVE_EMAIL_EVENTS' --include=*.ts apps/ \| grep -v dist/` → **2** (definice jména + konstruktor workera), žádné `.add`                                                                              | **SKRYTÉ** — tabulka `email_events` roste bez omezení, `ARCHIVE_CUTOFF_DAYS` nemá efekt.                                                                                                                            |
| **`run.data.smartChannel`**                                | `services/workflows/actions.ts:795`                                                                                                                         | Zapisuje se s komentářem „so the next node can read it". Nečte to nikdo.                                                                                                                                                                                                                                                        | `grep -rn 'smartChannel' --include=*.ts apps/ \| grep -v dist/` → **1** (jen ten zápis)                                                                                                                           | **SKRYTÉ** — write-only stav, opačný případ než `cascadeStep`.                                                                                                                                                      |
| **Fronta `batch-sender-transactional`**                    | worker `apps/workers/src/index.ts:73`                                                                                                                       | Nikdo do ní neprodukuje. Transakční pošta jde přímo do `mta-other` (`lib/queues.ts:241`), byť si v payloadu nese `stream: 'transactional'`.                                                                                                                                                                                     | `grep -rn 'BATCH_SENDER_TRANSACTIONAL\|batch-sender-transactional' --include=*.ts apps/` → **4 zásahy: 2 komentáře, definice jména, konstruktor workera**                                                         | **SKRYTÉ** — worker běží naprázdno; priorita transakčního streamu se v batch-senderu neuplatní.                                                                                                                     |
| **Exponenciální retry ladder pro soft bounce**             | `services/sending/retry-manager.ts` (celý modul)                                                                                                            | Žádná z šesti exportovaných funkcí nemá volajícího. Dokumentovaný rozvrh 1 min → 24 h neexistuje; reálně platí BullMQ `attempts: 3, exponential 5 s` z `mtaOpts`.                                                                                                                                                               | `grep -rn 'scheduleRetry' --include=*.ts apps/ \| grep -v 'dist/\|.test.\|retry-manager.ts:'` → **0** (totéž pro `getRetryState`, `isReadyForRetry`, `permanentlyFail`, `clearRetryState`, `calculateRetryDelay`) | **SKRYTÉ** — soft bounce dostane 3 pokusy během ~15 s místo šesti během 24 h.                                                                                                                                       |
| **Automatické zrušení sankcí (auto-unpause)**              | `services/abuse-detection/index.ts` → `expireStaleSanctions`                                                                                                | Bez volajícího.                                                                                                                                                                                                                                                                                                                 | příloha A                                                                                                                                                                                                         | **SKRYTÉ** — sankce nastavená auto-pausem sama nevyprší.                                                                                                                                                            |
| **Ingest DMARC reportů**                                   | worker `apps/workers/src/jobs/dmarc-imap-poll.ts:232`                                                                                                       | `scheduleDmarcImapPoll` má hned na začátku `if (!process.env.DMARC_IMAP_HOST) return;` a proměnná **není ani v `.env.example`**.                                                                                                                                                                                                | `grep -c 'DMARC_IMAP_HOST' .env.example` → **0**                                                                                                                                                                  | **SKRYTÉ** — cron se neregistruje, DMARC agregáty se nikdy nenačtou.                                                                                                                                                |

---

## Průchod 1 — Fronty

**Do kterých front se v `apps/api` nebo `apps/workers` přidává job (produkce).** Celý seznam:

`campaign-splitter` · `batch-sender` · `batch-sender-triggered` · `mta-gmail` · `mta-microsoft` · `mta-yahoo` · `mta-seznam` · `mta-volny` · `mta-centrum` · `mta-other` · `email` · `sms` · `webhook` · `viber-send` · `rcs-send` · `whatsapp-send` · `push-send` · `mobile-push-send` · `ab-winner` · `anomaly-detector` · `blacklist-monitor` · `dmarc-imap-poll`¹ · `external-feed-poll` · `invoice-reminder` · `ad-perf-sync` · `seo-rank-poll` · `social-publish` · `social-monitor` · `subscription-billing` · `video-transcode` · `warmup-advance` · `workflow-run-resume` · `daily-triggers` · `warehouse-sync-run` · `clickhouse-replicate` · `ticketing-day-of` · `ticketing-fill-house` · `ticketing-discover` · `campaign-dispatch` · `browse-abandonment-tick` · `scheduled-reports-run` · `segment-membership-refresh`

¹ produkce podmíněná `DMARC_IMAP_HOST` — bez ní se `.add` nikdy nezavolá.

**Pro které fronty je registrovaný processor** (`apps/workers/src/index.ts` + soubory, které startuje). Celý seznam:

`campaign-splitter` · `batch-sender` · **`batch-sender-transactional`** · `batch-sender-triggered` · `mta-gmail` · `mta-microsoft` · `mta-yahoo` · `mta-seznam` · `mta-volny` · `mta-centrum` · `mta-other` · **`archive-email-events`** · `seo-rank-poll` · `social-publish` · `social-monitor` · `invoice-reminder` · `ad-perf-sync` · `video-transcode` · `subscription-billing` · `anomaly-detector` · `viber-send` · `whatsapp-send` · `push-send` · `mobile-push-send` · `external-feed-poll` · `warmup-advance` · `dmarc-imap-poll` · `ab-winner` · `blacklist-monitor` · `webhook` · `rcs-send` · `email` · `sms` · `workflow-run-resume` · `daily-triggers` · `warehouse-sync-run` · `clickhouse-replicate` · `ticketing-day-of` · `ticketing-fill-house` · `ticketing-discover` · `campaign-dispatch` · `browse-abandonment-tick` · `scheduled-reports-run` · `segment-membership-refresh`

**Rozdíl obou množin:**

- **Produkce bez konzumenta: žádná.** `voice-call` je hraniční případ — `Queue` objekt existuje (`lib/queues.ts:70`) a je navázaný v mapě kanálů (`:288`), workera nemá žádného, ale jediná cesta, která by do něj přidávala, se nejdřív zeptá `queueHasConsumer` a vrátí chybu (`actions.ts:640-646`). Fakticky tedy nula producentů i nula konzumentů.
- **Konzument bez produkce: `archive-email-events`, `batch-sender-transactional`.** Obojí v tabulce výše.

**Nesoulad payloadů (produkuje se, konzumuje se, ale kontrakt nesedí)** — to je v tomhle repu častější než chybějící worker:

| fronta       | producent                                 | konzument čeká                                    | výsledek                             |
| ------------ | ----------------------------------------- | ------------------------------------------------- | ------------------------------------ |
| `email`      | `actions.ts:1021` `cascade-email`         | `campaignId` \| `templateId` \| `html`            | 400, po 3 pokusech fail              |
| `email`      | `actions.ts:758` `internal-notification`  | povinné `contactId` (zod) — payload ho nemá vůbec | zod throw                            |
| `email`      | `sales-sequences.ts:324` `sequence-email` | `html`/`text`, dostane `bodyHtml`/`bodyText`      | 400                                  |
| `sms`        | `actions.ts:1034` `cascade-sms`           | povinné `message` — payload ho nemá               | zod throw                            |
| `viber-send` | `actions.ts:994` `workflow-viber`         | `phone`, `type` — payload nemá ani jedno          | adaptér dostane `undefined` příjemce |
| `viber-send` | `actions.ts:1050` `cascade-viber`         | `body`, `type` — payload nemá                     | prázdný obsah                        |

Kontrolováno proti `WorkflowEmailJob` (`workflow-email-sender.ts:17-26`), zod schématům v `workflow-dispatch.ts:66-76` a `:174-185`, a `ViberSendJobData` (`viber-sender.ts:14-33`).

---

## Průchod 2 — Crony a repeatable joby

**18 repeatable jobů**, všechny registrované a všechny volající existující endpoint:

| job                                              | rozvrh                                   | volá                                                          |
| ------------------------------------------------ | ---------------------------------------- | ------------------------------------------------------------- |
| `anomaly-detector`                               | `*/5 * * * *`                            | `/internal/anomaly-detector/scan`                             |
| `blacklist-monitor`                              | každých 6 h                              | `/internal/blacklist-check`                                   |
| `dmarc-imap-poll`                                | `0 */4 * * *`                            | interní IMAP poll — **registruje se jen s `DMARC_IMAP_HOST`** |
| `external-feed-poll`                             | `0 * * * *`                              | `/internal/external-feeds/poll`                               |
| `invoice-reminder`                               | `0 8 * * *`                              | `/internal/commerce/invoice-reminders`                        |
| `ad-perf-sync`                                   | `0 7 * * *`                              | `/internal/ads/sync-performance`                              |
| `seo-rank-poll`                                  | `0 6 * * *`                              | `/internal/seo/rank-poll`                                     |
| `social-publish`                                 | `* * * * *`                              | `/internal/social/dispatch-due`                               |
| `social-monitor`                                 | `*/15 * * * *`                           | `/internal/social/monitor-poll`                               |
| `subscription-billing`                           | `*/5 * * * *`                            | `/internal/subscriptions/generate-due`                        |
| `video-transcode`                                | každých 30 s                             | `/internal/video/pending`                                     |
| `warmup-advance`                                 | `5 0 * * *`                              | `/internal/sending/warmup/advance-all`                        |
| `workflow-run-resume`                            | `* * * * *`                              | `/internal/workflow/process-runs`                             |
| `daily-triggers`                                 | `0 6 * * *`                              | `/internal/triggers/daily-run`                                |
| `warehouse-sync-run`                             | `15 * * * *`                             | `/internal/warehouse-sync/run-due`                            |
| `clickhouse-replicate`                           | `* * * * *`                              | `/internal/clickhouse/replicate`                              |
| `ticketing-day-of` / `-fill-house` / `-discover` | `* * * * *` / `0 10 * * *` / `0 9 * * 1` | `/internal/ticketing/*/tick`                                  |
| `campaign-dispatch`                              | `* * * * *`                              | `/internal/campaigns/dispatch-scheduled`                      |
| `browse-abandonment`                             | `*/15 * * * *`                           | `/internal/browse-abandonment/tick`                           |
| `scheduled-reports`                              | `5 * * * *`                              | `/internal/scheduled-reports/run-due`                         |
| `segment-membership`                             | `*/5 * * * *`                            | `/internal/segments/refresh-membership`                       |

**Job, který volá neexistující endpoint: žádný.** Všech 65 nalezených `/api/v1/internal/*` cest existuje.

**Endpointy, které nikdo nevolá (0 volajících v `apps/workers`, `apps/engine`, `apps/web`, `apps/api/src/services`, `apps/sms-gateway`, `apps/voice-bot`, `packages`):**

`/internal/channel-scoring/refresh` · `/internal/channel-scoring/refresh-all` · `/internal/engagement-score/refresh` · `/internal/engagement-score/refresh-all` · `/internal/predictive/refresh` · `/internal/predictive/refresh-all` · `/internal/rfm/refresh` · `/internal/rfm/refresh-all` · `/internal/coupons/allocate-batch` · `/internal/clickhouse/migrate` · `/internal/status-page/incident` · `/internal/status-page/open` · `/internal/status-page/resolve`

U prvních osmi to **není** mrtvá funkce — příslušné služby volá `daily-run` přímo v procesu (`routes/v1/internal/triggers.ts:86-93`: `refreshAllOrgsRfm`, `refreshAllOrgsPredictions`, `refreshAllOrgsChannelScores`, `refreshAllOrgsEngagement`). Endpointy jsou jen nepoužité ruční spouštěče. `allocate-batch` je nadbytečný podobně: kupóny řeší batch-sender přímo importem `resolveEmailCouponTags` (`batch-sender.ts:319-321`). Zbývají tedy jako opravdu nezapojené `clickhouse/migrate` a tři `status-page/*`.

**K tvojí indicii o `per-contact-sto.ts`:** potvrzeno a je to horší, než jsi čekal. Nejde jen o chybějící cron — `computeContactSendTime`, `getOrgHourPrior` i `nextOptimalSendTime` mají **nula** volajících mimo `routes/v1/per-contact-sto.ts`. Predikci tedy nespouští nic: ani cron, ani worker, ani `apps/web`. Detail v hlavní tabulce.

---

## Průchod 3 — Env proměnné, které tiše vypínají funkci

`grep -rhno "process\.env\.[A-Z0-9_]*" --include=*.ts apps/api/src apps/workers/src | sort -u` → **163 různých proměnných**. Ve validačním schématu `apps/api/src/config/env.ts` je jich **52**; zbylých **124** se čte syrově, bez jakékoli kontroly při startu.

### (a) Chybí hodnota → aplikace spadne nebo hlasitě odmítne start

Sem patří to, co prochází `config/env.ts`: `DATABASE_URL`, `REDIS_URL`, `JWT_SECRET` (bez defaultu, zod padá), plus `prodRequired(...)` skupina — `SESSION_SECRET`, `INTERNAL_API_SECRET`, `TRACKING_SECRET`, `ASSET_SIGNING_SECRET`, `INBOUND_EMAIL_SECRET`, `FORM_AUTOFILL_SECRET`, `PREFERENCE_CENTRE_SECRET`, `FBL_WEBHOOK_SECRET`, `DMARC_INBOUND_SECRET`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`, verify-tokeny Meta/WhatsApp/Facebook. Dále `HIPAA_FIELD_KEY` — `encryptPhiValue` bez něj **throwne** (`services/compliance/hipaa.ts:54-61`), a `TWILIO_*` v `voice/call-manager.ts:278-280` (`throw new Error('Twilio credentials not configured')`).

### (b) Chybí hodnota → funkce tiše nic nedělá

| proměnná                                            | místo                                            | co se stane                                                           | v `.env.example` | v compose | v CI   |
| --------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------- | ---------------- | --------- | ------ |
| `GEOIP_API_URL`                                     | `lib/geo.ts:68-69`                               | `resolveGeo` vrací `null`, geo statistiky prázdné                     | ano              | **ne**    | **ne** |
| `LITMUS_API_KEY`                                    | `services/preview/providers.ts:169-170`          | tichý fallback na `mockProvider` s fiktivními URL                     | ano              | ano       | **ne** |
| `SENDING_IPS`                                       | `apps/engine/main.go:43`                         | warmup enforcement se nezapne                                         | **ne**           | **ne**    | **ne** |
| `VERP_BOUNCE_DOMAIN`                                | `batch-sender.ts:195`                            | `?? ''` → Return-Path se nenastaví, out-of-band bounce nedohledatelný | **ne**           | **ne**    | **ne** |
| `DMARC_IMAP_HOST`                                   | `dmarc-imap-poll.ts:232`                         | cron se vůbec neregistruje                                            | **ne**           | **ne**    | **ne** |
| `ZOOM_ACCOUNT_ID`/`_CLIENT_ID`/`_CLIENT_SECRET`     | `meetings/video-links.ts:50`                     | rezervace potvrzena bez odkazu                                        | ano              | **ne**    | **ne** |
| `MICROSOFT_TENANT_ID`/`_CLIENT_ID`/`_CLIENT_SECRET` | `meetings/video-links.ts:117`                    | totéž pro Teams                                                       | ano              | **ne**    | **ne** |
| `APNS_KEY_P8`/`_KEY_ID`/`_TEAM_ID`/`_BUNDLE_ID`     | `push/mobile-transport.ts:61`                    | `null` → zařízení se zapíše jako „skipped"                            | ano              | **ne**    | **ne** |
| `FCM_SERVICE_ACCOUNT`                               | `push/mobile-transport.ts:163,170,177`           | totéž; `catch { return null }` polyká i rozbitý JSON                  | ano              | **ne**    | **ne** |
| `PARTNER_PROVISION_SECRET`                          | `routes/v1/partner.ts:12-13`                     | partnerské provisioning API je vypnuté (`return false`)               | **ne**           | **ne**    | **ne** |
| `TWILIO_ACCOUNT_SID` + `_AUTH_TOKEN`                | `routes/v1/phone/numbers.ts:267-268`, `:327-328` | vyhledání čísel vrátí `[]`, druhé místo `return;` bez logu            | ano              | **ne**    | **ne** |
| `DATA_REGION_OVERRIDES`                             | `services/data-residency/index.ts:24-29`         | `{}` a `catch` → `{}`                                                 | **ne**           | **ne**    | **ne** |
| `ARCHIVE_CUTOFF_DAYS`                               | `archive-email-events.ts:22`                     | default 30 — ale job stejně nikdo nespouští                           | **ne**           | **ne**    | **ne** |

**Konfigurační past mimo obě kategorie:** existují dvě různá jména pro tutéž věc a každé má jiný default — `SYSTEM_EMAIL_FROM` → `noreply@forgemsg.com` (`services/gdpr/per-purpose-doi.ts:103`) a `SYSTEM_FROM_EMAIL` → `no-reply@forgemsg.io` (`services/identities/index.ts:40`). Nastavením jedné z nich se druhá cesta nezmění.

---

## Průchod 4 — Čtený, ale nezapisovaný stav

Prošel jsem klíče čtené z `run.data` (workflow běhy), z `job.data` (BullMQ payloady, viz tabulka nesouladů v průchodu 1) a z JSONB histogramů engagementu.

| klíč                                                                   | čte se                                                 | zapisuje se                                                                          | verdikt                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `run.data.cascadeStep`                                                 | `actions.ts:943`, `:947`                               | `actions.ts:972`                                                                     | **na tomto commitu už zapisuje.** Tvoje zadání popisuje stav před `e12ef8d`; na `3d8b581` je zápis v produkčním kódu a test `3d8b581` ho pohání celým cyklem. Nechávám tu, aby bylo zjevné, že jsem to ověřoval znovu a ne přebíral. |
| `run.data.invoiceId`                                                   | `actions.ts:1388`                                      | **nikde**                                                                            | mrtvý — viz hlavní tabulka                                                                                                                                                                                                           |
| `run.data.smartChannel`                                                | **nikde**                                              | `actions.ts:795`                                                                     | opačný případ: write-only                                                                                                                                                                                                            |
| `run.data.order` / `.order_amount` / `.amount`                         | `actions.ts:97`, `:904`, `:1494`                       | `triggers.ts:229` přes `onOrderPlaced` (payload z `services/ecommerce/index.ts:421`) | v pořádku                                                                                                                                                                                                                            |
| `run.data.eventTitle`, `.event_url`, `.ticket_url`, `.externalEventId` | `actions.ts:93` a merge tagy                           | `services/ticketing/cron.ts:69-74`                                                   | v pořádku                                                                                                                                                                                                                            |
| `run.data.cart_url`, `.review_url`                                     | šablony `seed-workflows.ts:42`, `:52`                  | **nikde**                                                                            | mrtvý — renderuje se prázdno                                                                                                                                                                                                         |
| `contact_engagement.open_hour_histogram`                               | `send-optimization/index.ts:88` (`bestHourForContact`) | jen `send-optimization/index.ts:39` z ručního endpointu                              | fakticky mrtvý — pixel otevření ho neplní                                                                                                                                                                                            |
| `dedicated_ips.reputation_score`                                       | `dedicated-ips/index.ts:280`, `:417`, `:439`           | jen `:582` uvnitř `updateReputation`, které nemá volajícího                          | mrtvý                                                                                                                                                                                                                                |
| `run.splitBranch`, `run.converted`, `run.conversionValue`              | `conversion-suppression.ts`, analytika                 | `executor.ts:114-129` po každém uzlu                                                 | v pořádku                                                                                                                                                                                                                            |

Zápis pouze v testovacím fixture jsem nikde jinde než u `cascadeStep` (a tam už neplatí) nenašel.

---

## Průchod 5 — Mrtvé služby

Metoda: 1929 řádků `export (async )?function` v `apps/api/src/services/**` → 1863 unikátních jmen. Korpus 1383 souborů `.ts`/`.tsx` napříč `apps/` a `packages/` **bez** `node_modules`, `dist`, `*.d.ts`, `*.test.*`, `*.spec.*`. Index přes `grep -oHw -F -f` (7785 výskytů), pak agregace: pro každé jméno počet výskytů v souborech **jiných** než definičním.

Výsledek:

- **251 jmen má nula volacích míst mimo vlastní soubor a zároveň nula použití uvnitř něj** — skutečně nereferencované. Rozprostřeno do **126 souborů**, viz příloha A.
- **212 jmen má nula volacích míst mimo vlastní soubor, ale používají se uvnitř** — jen zbytečně exportované, ne mrtvé. Neuvádím je.
- Zbytek (1400) má aspoň jedno externí volací místo.

Přesnost metody: v `apps/api/src/services` není ani jeden `export * from` barrel (`grep -rn "^export \* from" --include=*.ts apps/api/src/services` → **0**), takže se přes barrel nic neschovává. 18 pojmenovaných re-exportů (`export {`) se v indexu počítá jako externí výskyt — u nich tedy hrozí falešně _negativní_ výsledek, ne falešně pozitivní.

Nejzávažnější shluky:

- **17 workflow triggerů bez jediného volajícího** (`services/workflows/triggers.ts`): `onAddedToCart`, `onCheckoutStarted`, `onCompanyEvent`, `onConsentExpired`, `onConsentGranted`, `onConsentRevoked`, `onDealCreated`, `onDealLost`, `onDealStageChanged`, `onDealWon`, `onEmailLinkClick`, `onLoyaltyPointsEarned`, `onLoyaltyRewardRedeemed`, `onTicketEvent`, `processQueuedWorkflowEvents`, `processStaleDealTriggers`. Ověřeno i jednotlivě: `grep -rn "\bonDealWon\b" --include=*.ts --include=*.tsx apps/ packages/ | grep -v "dist/\|triggers.ts:"` → **0**, totéž pro ostatní. Praktický dopad je ale omezený tím, že `consent_*`, `company_event`, `ticket_event` a `email_link_click` **nejsou v enumu** `workflowTriggerTypeEnum` ani v `triggerTypeValues` v `routes/v1/workflows.ts` (v kódu jsou přetypované `as never`) — takový workflow tedy nejde ani založit. Skutečně bolí trojice, kterou **API založit dovolí**: `loyalty_points_earned`, `loyalty_reward_redeemed` a `lifecycle_stage_changed`. `loyalty_tier_up` se spustí, ale jen když body přidělí uzel workflow (`actions.ts:1330`), ne když je přidělí loyalty služba.
- **Opačná asymetrie:** `onSegmentEntered` / `onSegmentExited` se z cronu membership skutečně volají (`services/segments/membership.ts:78`), ale `segment_entered` a `segment_exited` **chybí v `triggerTypeValues`** (`routes/v1/workflows.ts:39-53`), takže si takové workflow nikdo nezaloží. Funkční odpalovač bez dosažitelného spotřebitele.
- **Odesílací cesta** (celé moduly bez volajícího): `sending/retry-manager.ts` (6 funkcí), `sending/ip-warmup.ts` → `checkWarmupAllowance`, `sending/send-rate.ts` → `exceedsRate`, `sending/bounce-processor.ts` → `shouldSuppressAfterSoftBounces`, `dedicated-ips/index.ts` → `recordIpSend` / `updateReputation` / `dailyIpMaintenance`, `domains/warmup-scheduler.ts` → `getIpWarmupStatus` / `getWarmupBatchLimit` / `recordIpSend`, `domains/dkim.ts` → `signEmailDkim` (podpis dělá Go engine, tohle je duplikát).
- **Duplicitní modul, ne chybějící funkce:** `services/analytics/anomaly-detector.ts` (`runAnomalyCheckForOrg`, `runAnomalyCheckAllOrgs`) je mrtvý, protože cron používá druhou implementaci `services/deliverability/anomaly-detector.ts` (`scanForAnomalies`, `routes/v1/internal/anomaly-detector.ts:12`). Anomálie se tedy detekují.

---

## Chybné indicie

Co jsem čekal, že bude rozbité, a zapojené to je:

1. **`run.data.cascadeStep`** — zadání ho uvádí jako známý případ. Na `3d8b581` je opravený: zápis `run.data = { ...cascadeData, cascadeStep: currentStep + 1 }` je v produkčním kódu na `actions.ts:972` a test ho žene celým cyklem bez ručního dosazování.
2. **Fronta `webhook`** — má konzumenta (`jobs/webhook-deliver.ts`, startuje se v `index.ts`) a kontrakt sedí přesně: producent posílá `{deliveryId}` (`services/webhooks/index.ts:310`), worker čte `{deliveryId}` (`webhook-deliver.ts:33`).
3. **`/internal/predictive|rfm|engagement-score|channel-scoring/refresh*`** — nula HTTP volajících, ale samotné funkce běží denně v procesu z `daily-run` (`triggers.ts:86-93`). Endpointy jsou nadbytečné, featury živé.
4. **`/internal/coupons/allocate-batch`** — také nula volajících, ale kupóny se alokují jinou cestou: batch-sender importuje `resolveEmailCouponTags` napřímo (`batch-sender.ts:29`, volá se na `:319-321` pro subject, HTML i text).
5. **`HIPAA_FIELD_KEY`** — čekal jsem tiché ukládání PHI v plaintextu. Opak: `encryptPhiValue` explicitně **throwne** a v komentáři je popsáno, že dřív tiše propouštěl.
6. **RCS bez `RCS_SINCH_*`** — nelže. `sendRcs` vrátí `{status:'failed', error:'RCS Sinch not configured'}` a worker zapíše zprávě stav `failed` (`rcs-sender.ts:34`).
7. **`CLICKHOUSE_ENABLED`** — vypnuto je dokumentovaný stav, analytika padá zpátky na Postgres (`services/analytics/clickhouse/client.ts:6-7`).
8. **Naplánované kampaně, ISP throttle, VERP, scheduled reports, geo write-path, scopes API klíčů, `onListSubscribe` / `onTagAdded`** — všechno, co červencový audit hlásil jako rozbité, jsem přeověřoval a je to zapojené. Detaily jsou v `docs/MAILCHIMP-VS-MAILFORGE-2026-08-19.md`, tady je neopakuju.
9. **Mobilní push bez APNs/FCM** — zařízení se zaznamená jako `skipped`, ne jako doručené; chování je popsané v hlavičce `mobile-push-sender.ts:5-6`.

---

## Příloha A — 251 nereferencovaných exportů ve 126 souborech

Cesty relativní k `apps/api/src/services/`.

- `abuse-detection/index.ts` — expireStaleSanctions
- `ads/providers/google/pure.ts` — chunkGoogle<T> computeGoogleStats
- `ads/providers/meta/pure.ts` — chunkMeta<T> computeMetaStats
- `ads/providers/sklik/pixel.ts` — buildAudienceFromPixel newVisitorId
- `ads/providers/sklik/pure.ts` — chunk<T> computeStats
- `ads/providers/tiktok/pure.ts` — buildTikTokPayload chunkTikTok<T> computeTikTokStats
- `ads/pure.ts` — attributeConversion buildUtmUrl computeAdPerformance formatAudienceRow
- `ai-agents/pure.ts` — aggregateIntent scoreDealHealth scoreProspect
- `ai-agents/seo-strategist.ts` — generateSeoStrategy
- `ai-agents/social.ts` — generateSocialDrafts
- `ai-analytics/pure.ts` — suggestChartType
- `ai-analytics/saved-queries.ts` — recordRun
- `ai-sales/deal-risk.ts` — scoreDealRisk
- `ai-sales/pure.ts` — scoreDealRisk weightedSentiment
- `ai-sales/sentiment.ts` — aggregateSentiment
- `analytics/anomaly-detector.ts` — runAnomalyCheckAllOrgs
- `analytics/clickhouse/client.ts` — chQuery<T>
- `app-studio/index.ts` — dispatchToApps
- `archive/email-events.ts` — listArchivedFiles
- `associations/index.ts` — cleanupEntity
- `auth/permission-sets/index.ts` — assignToUser createPermissionSet deletePermissionSet getEffectivePermissions listPermissionSets unassignFromUser updatePermissionSet
- `auth/sessions.ts` — revokeAllUserSessions
- `billing/credits.ts` — checkSufficientCredits deductCredits refundCredits
- `billing/currency.ts` — getOrgBillingCurrency validateBillingCurrency
- `billing/plans.ts` — calculateOverageCost getContactPlan getOverageRate isSendAllowed
- `billing/subaccounts-pure.ts` — canPerform effectiveRole rollupUsage rootOrgId
- `billing/subaccounts.ts` — buildInvoiceLineItems getBillingRootOrgId getEffectiveRole getEffectiveSubscription
- `blog/index.ts` — publishDuePosts
- `calculated-props/index.ts` — writeCachedValues
- `campaigns/click-actions.ts` — wrapLinkWithAction
- `campaigns/email-coupon-merge.ts` — previewEmailCouponTags
- `campaigns/index.ts` — incrementCampaignStat markCampaignSent
- `cdp/pure.ts` — resolveTrait shouldMergeProfiles signalConfidence
- `cdp/traits.ts` — bumpCommerceTraits
- `cdp/unified-profile.ts` — getUnifiedProfiles invalidateProfilesBulk
- `commerce/currency.ts` — formatCurrency isSupportedCurrency sumInTargetCurrency
- `commerce/invoicing-cz.ts` — formatCzInvoiceNumber formatCzk isValidCzVatRate isValidDic isValidIco isValidPsc
- `commerce/invoicing.ts` — getOverdueInvoices
- `commerce/isdoc-export.ts` — renderIsdocInvoice
- `commerce/payments.ts` — createOrGetStripeCustomer createQuotePaymentIntent
- `commerce/products.ts` — buildLineItemsFromProducts
- `commerce/pure.ts` — computeProration computeQuoteTotals
- `commerce/revenue-schedule.ts` — computeDeferredRevenue computeMrrMovement
- `commerce/spayd.ts` — buildSpaydString
- `commerce/subscriptions.ts` — computeProration
- `compliance/hipaa.ts` — decryptPhiFields encryptPhiFields getPhiFieldKeySet logPhiAccess redactPhiFields
- `crm/linkedin-inmail.ts` — extractLinkedInUrn
- `crm/pipelines.ts` — getDefaultPipeline
- `crm/pure.ts` — computeAverageCycleDays computeRepPerformance computeStageDistribution computeWinLoss
- `crm/sales-sequences.ts` — processDueSequenceSteps
- `data-ops/index.ts` — executePipeline
- `data-quality/normalizers.ts` — normaliseContact
- `data-residency/index.ts` — assertResourceRegion
- `data-sync/engine.ts` — applyPlan listEnabledMappings
- `dedicated-ips/index.ts` — dailyIpMaintenance recordIpSend updateReputation
- `deliverability/bot-detection.ts` — backfillScores
- `deliverability/pure.ts` — aggregateReputation
- `domains/dkim.ts` — signEmailDkim
- `domains/warmup-scheduler.ts` — getIpWarmupStatus getWarmupBatchLimit recordIpSend
- `editor/amp-renderer.ts` — buildMimePartOrder
- `editor/connected-content.ts` — fetchConnectedContent
- `editor/dark-pattern-detector.ts` — detectDarkPatterns
- `editor/merge-tag-validation.ts` — invalidateMergeKeys
- `editor/templates/index.ts` — getAllCategories
- `field-permissions/pure.ts` — findUnwritableField fpermCacheKey mergeRule
- `forms/conditional-logic.ts` — filterVisibleFields
- `helpdesk/pure.ts` — channelTrust chooseReuseTicket normalizeInboxIdentity
- `helpdesk/routing.ts` — releaseTicket
- `i18n/pure.ts` — applyTranslations<T> collectTextNodes localeFallbackChain selectVariant<T>
- `identity-resolution/index.ts` — getVisitorFingerprint
- `identity-resolution/pure.ts` — decayConfidence isIdentifying
- `in-app/index.ts` — getInAppMessage
- `inbound-email/index.ts` — appendReplyToTicket
- `inbox/instagram.ts` — fetchInstagramThread
- `inbox/messenger.ts` — fetchMessengerProfile
- `integrations/flexibee.ts` — pushContactToFlexibee
- `lead-scoring/model.ts` — scoreLeadProbability
- `lifecycle/pure.ts` — suggestedStageFromSignals
- `loyalty/ledger.ts` — computeExpiresAt
- `loyalty/programs.ts` — computeFixedExpiryDate computeRollingExpiryDate createProgram deleteProgram getNextTier getProgram getTierMultiplier listPrograms resolveTier updateProgram validateExpiryPolicy validateTiers
- `loyalty/pure.ts` — applyTierMultiplier computeExpiresAt getNextTier getTierMultiplier resolveTier validateExpiryPolicy validateTiers
- `meetings/pure.ts` — computeFreeSlots intersectFreeSlots sliceIntoSlots
- `meetings/round-robin.ts` — getTeamAvailability
- `meetings/workflows.ts` — scheduleMeetingReminders
- `multivariate-tests/index.ts` — incrementVariantStats processDueTests
- `phone/pure.ts` — normalizeE164 selectHuntPool
- `phone/recording.ts` — handleRecordingCompleted
- `phone/routing.ts` — resolveIvrChoice setAgentPresence
- `phone/transcription.ts` — transcribeCall
- `product-catalog/feed-ingestion.ts` — listDueFeeds
- `push/mobile-pure.ts` — isTokenInvalidError
- `push/mobile-transport.ts` — \_resetTokenCaches
- `push/mobile.ts` — prepareContactSend
- `rag/embedding.ts` — activeProvider
- `rcs/index.ts` — buildCarousel
- `rcs/providers/index.ts` — isRcsConfigured
- `revenue-attribution/ad-source.ts` — attributeRevenueToAd extractAdAttribution
- `sandboxes/pure.ts` — buildSandboxSlug canParentSpawnSandbox chunkRange clampSeedCount shouldNoOpSend syntheticContactEmail
- `sending/bounce-processor.ts` — shouldSuppressAfterSoftBounces
- `sending/from-domain.ts` — fromAddressDomain
- `sending/ip-warmup.ts` — checkWarmupAllowance
- `sending/retry-manager.ts` — calculateRetryDelay clearRetryState getRetryState isReadyForRetry permanentlyFail scheduleRetry
- `sending/send-rate.ts` — exceedsRate
- `seo/pure.ts` — extractInternalLinks
- `signup-forms/autofill.ts` — encryptContactId resolveSessionToken setTrackingMapping
- `signup-forms/index.ts` — trackVariantSubmit
- `site-messages/pure.ts` — canShowMessage
- `sms-keywords/index.ts` — deleteKeyword
- `sms/compliance.ts` — getConsent
- `social/pure.ts` — fitForPlatform isSupportedPlatform mergeHashtags pickBestTimeSlots validateCrossPost
- `status-page/index.ts` — \_setClient
- `surveys/nps-csat.ts` — computeCsatScore evaluateResponseRules
- `system-emails/index.ts` — renderDoiConfirmEmail renderEmailVerificationEmail renderPasswordResetEmail renderTeamInviteEmail
- `teams/index.ts` — assertTeamWrite resolveTeamContext
- `teams/pure.ts` — anyCrossTeamAccess canWriteToTeam deriveBypass membershipCacheKey normaliseTeamIds planTeamScope
- `ticketing/day-of.ts` — computeDayOfSchedule
- `ticketing/fill-the-house.ts` — getEventForYield
- `ticketing/recipes.ts` — recipesByGroup recipesForTrigger
- `ticketing/recommendations.ts` — attendedByContact
- `whatsapp/compliance.ts` — checkWaCompliance getWaPhoneNumber hasOpenConversationWindow openConversationWindow
- `whatsapp/rich-messaging-test-exports.ts` — buildButtonPayloadForTest buildListPayloadForTest buildMediaPayloadForTest
- `whatsapp/templates.ts` — incrementTemplateUsage
- `workflows/actions.ts` — \_\_resetQueueConsumerCache
- `workflows/condition-rules.ts` — isConditionSupported
- `workflows/conversion-suppression.ts` — hasContactConverted
- `workflows/triggers.ts` — onAddedToCart onCheckoutStarted onCompanyEvent onConsentExpired onConsentGranted onConsentRevoked onDealCreated onDealLost onDealStageChanged onDealWon onEmailLinkClick onLoyaltyPointsEarned onLoyaltyRewardRedeemed onTicketEvent processQueuedWorkflowEvents processStaleDealTriggers
