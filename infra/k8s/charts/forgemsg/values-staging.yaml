global:
  environment: staging
  domain: staging.example.invalid
  image:
    repositoryPrefix: forgemsg-org/forgemsg

api:
  replicaCount: 2
  ingress:
    host: api.staging.example.invalid

voiceBot:
  replicaCount: 1
  ingress:
    host: voice.staging.example.invalid

workers:
  replicaCount: 2
